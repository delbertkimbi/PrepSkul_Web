import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "SBC Packages & Pricing | Summer Build Camp 2026",
  description: "Compare Explorer, Creator and Innovator packages for Summer Build Camp 2026.",
}

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return children
}
