import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Summer Build Camp FAQ | Dates, Fees and Participation",
  description: "Answers for parents about SBC 2026 dates, ages, fees, devices, Buea onsite participation, online access and Demo Day.",
  alternates: { canonical: "/faq" },
}

export default function Layout({ children }: { children: React.ReactNode }) { return children }
